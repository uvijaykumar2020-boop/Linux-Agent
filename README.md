# Linux-Agent
# Go to ec2 instance & install the git package
# sudo yum install -y git
# Cloing the github from your repo on the ec2 instance by copying the repo URL found in the github https clone
# git clone <repo_URL>
# git pull origin master 